{
  "name": "email-sender-bot",
  "version": "1.0.0",
  "description": "Email Sender Bot with XLSX upload, scheduling, and live logs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore data/ --ignore uploads/ server.js",
    "clear": "npx kill-port 3000"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.2"
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "nodemon": "^3.0.2"
  }
}